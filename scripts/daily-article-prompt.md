# Retired Claude-era prompt

This prompt is not a production publishing path.

The active one-article-per-day writer is:

`prompts/codex-daily-article-writer.md`

The guarded production entry point is:

`scripts/run-codex-daily.sh --live`

It uses Krista's ChatGPT subscription with GPT-5.6 Luna, low reasoning, and standard service tier. Do not restore the former Claude batch workflow.
